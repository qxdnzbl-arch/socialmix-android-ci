# SocialMix Core Stable SPEC

## One-Sentence Concept
在不扩张产品范围的前提下，把 SocialMix 现有真实账号、好友、一对一文字聊天做成打开即有内容、发送立即出现、接收实时到达、弱网不把整页卡成“加载中”的可用核心版本。

## Background / Motivation
当前主要问题不是视觉细节，而是网络请求直接阻塞页面状态：消息页和“我”页会长时间显示加载中；聊天发送依赖服务器响应后才稳定呈现；网络波动时应用显得像未完成 Demo。先把现有核心链路做稳，再进入朋友动态等后续功能。

## Target User And Use Scenario
两个真实用户已经注册并成为好友。用户打开 App 能立即看到上一次成功同步的账号和会话内容；进入聊天后发送文字消息立即出现在本机，对方通过 Realtime 收到；后台再与 Supabase 对账。

## Core User Path
打开 App → 立即显示本地可用状态 → 后台刷新账号/会话 → 打开好友聊天 → 本机立即显示发送消息 → Supabase 持久化 → 对方 Realtime 收到 → 重新进入仍能看到历史消息。

## Platform & Technical Constraints
- Android 原生 Jetpack Compose。
- 继续使用现有 Supabase、publishable key 与 RLS。
- 不更换现有账号、好友、会话、消息表结构。
- 本地缓存只作为“先显示”的副本，服务器仍是最终事实源。
- 网络请求必须有明确超时，不允许无限阻塞页面。

## Phase 1 Scope
- 账号资料本地缓存，登录后/再次打开时优先立即显示。
- 会话列表本地缓存，已有数据时禁止整页退回“正在加载”。
- 最近文字消息本地缓存，重新进入聊天时先显示缓存再后台刷新。
- 点击发送后立即清空输入框并插入本地消息，不等待服务器。
- 服务器确认后用真实消息替换本地临时消息，避免重复。
- Supabase Realtime 作为接收主链路；慢速网络刷新只作兜底。
- 会话列表后台刷新频率降低，避免 1.5 秒持续轮询。
- 网络请求增加超时，失败时保留已有缓存，不把可用页面清空。
- 保留现有注册、登录、好友搜索、好友申请、接受/拒绝、联系人、退出登录能力。

## Explicitly Out Of Scope
- 本阶段不新增图片、视频、语音消息。
- 不新增群聊。
- 不新增朋友动态/朋友圈/推荐流后端。
- 不新增点赞、评论、收藏、推送、已读回执、正在输入。
- 不接入第三方小红书账号或抓取小红书内容。

## Assumptions
- 用户已明确同意先把底层消息链路和加载问题彻底做好，再扩展未完成功能。
- 已存在的 Supabase 数据结构和 Phase 1 测试账号继续可用。

## Scenario Acceptance Tests
### Scenario: 再次打开不被加载页挡住
Given: 用户此前至少成功同步过一次账号和会话
When: 重新打开 App
Then: “我”页和消息页立即显示本地缓存内容；后台刷新失败也不清空已有内容。

### Scenario: 发送立即出现
Given: A、B 已经是好友并进入同一聊天
When: A 点击发送一条文字消息
Then: A 本机在网络请求完成前就能看到该消息且输入框立即清空；服务器成功后不出现重复气泡。

### Scenario: 对方实时收到
Given: B 正停留在同一聊天页且 Realtime 已订阅
When: A 发送文字消息
Then: B 无需手动刷新即可收到；8 秒级网络刷新只是兜底，不是主接收路径。

### Scenario: 弱网仍可用
Given: 本地已有账号、会话和消息缓存
When: Supabase 请求超时或短暂失败
Then: 现有内容继续可浏览；应用不无限停在整页“正在加载”。

### Scenario: 历史消息保留
Given: A/B 已成功发送过消息
When: 退出聊天并重新进入或重建 API 实例
Then: 先显示最近缓存，联网后与服务端历史消息对齐。

### Scenario: 现有核心功能不回归
Given: 未登录或已登录用户
When: 完成注册/登录、添加好友、接受/拒绝申请、打开联系人、聊天、退出登录
Then: 现有 Phase 1 流程仍可正常完成，权限边界不被破坏。

## Handoff
请根据这份 SPEC 实现 Phase 1。只做 Phase 1，不做 Explicitly Out Of Scope 里的内容。完成后按 Scenario Acceptance Tests 自测，只有全部核心场景通过才产出交付 APK。
