# 可话替代｜核心母版设计任务 v1

## 0. 这轮只做什么
这轮不是做完整App，也不是打包原生应用。
只做3个能决定整套产品体验的核心母版，并把它们做成同一个可运行单文件HTML：
1. 表达编辑体验
2. 共鸣首页
3. TA个人主页

这3页通过后，其他页面（聊天、我、自我发现、共鸣详情、设置）只能沿用同一套视觉和交互系统扩展。

## 1. 产品本质
这是一个“低压力表达驱动的共鸣软件”。

表达本身就是完整价值，不要求用户一定社交。
共鸣和聊天都是可选能力。

必须保持：
- 表达 ≠ 被发现 ≠ 共鸣 ≠ 聊天 ≠ 持续关系
- 共鸣不是无限推荐流
- 聊天不是共鸣的必然下一步
- 用户可以只表达、暂时不共鸣、暂时不聊天
- 自我展示不影响曝光权重
- 不做点赞、粉丝、热度、排行、匹配百分比、成长等级

## 2. 当前已有视觉方向（作为基线，不是要求机械照抄）
当前原型已经形成一套较克制的视觉语言：
- 浅米白背景
- 深灰黑正文
- 低饱和绿色作为强调色
- 极少装饰
- 圆角、留白、细分隔
- 安静、轻、不过度“玻璃感”

这轮可以优化比例、密度、组件细节、层级和动效，让它更高级、更和谐、更顺手；但不能把产品改成炫技型视觉，也不能为了“设计感”牺牲清楚、可点和低压力。

## 3. 全局硬约束
### 3.1 单手好用
- 主要高频操作放在屏幕下半区
- 主要点击区域至少44–48px
- 输入时不缩放页面、不把核心UI挤走、不出现跳布局
- 返回后保持原位置/状态

### 3.2 统一
- 同类内容使用同一套卡片骨架
- 同一横向网格
- 同一字号层级
- 同一间距体系
- 同一圆角体系
- 不靠“突然巨大/突然很小/突然超大留白”制造层级

### 3.3 敞亮但不空
- 不允许满屏文字墙
- 也不允许为了“极简”把必要信息删到看不懂
- 首屏只露够用户做下一步判断的信息，细节下一层再看
- 视觉丰富度主要来自头像、背景图、真实媒体内容、清晰层级，而不是堆装饰

### 3.4 低压力
- 不催“多发一点”“去认识更多人”“保持活跃”
- 不用强红点、倒计时、进度条、关系维持提醒
- 共鸣页不能有“下一批/刷新更多/无限下拉刷人”

## 4. 母版A：表达
### 目标
让用户“想说的时候，几乎不用思考就能开始表达”。

### 当前问题
当前原型把大输入框放在页面上方，手难够；点进去以后标题变小、页面状态变化、底栏隐藏，体感不稳定。

### 新结构
表达主页不显示历史表达卡（历史已经在“我”页）。
只保留：
- 极轻的日期/环境信息（可选）
- 大面积安静背景
- 底部一个唯一表达入口
- 四个底部主导航：表达｜共鸣｜聊天｜我

表达入口文案不要命令式，不要重复“今天想说点什么”。需要轻松、自然、像“随时可以开始”，具体文案可优化。

### 点击表达入口后
进入独立编辑页：
- 直接获得输入焦点
- 页面结构稳定
- 键盘出现后不缩、不跳
- 顶部只有关闭/返回与发布
- 正文区域占主角
- 底部靠近键盘处可添加：图片 / 音乐 / 作品（电影、游戏、书）
- 第一版一条表达最多附一种主要媒体
- 媒体是表达材料，不抢正文主体

### 表达范围
编辑页轻量显示一个范围胶囊：
- 用于共鸣（默认）= 主页可见 + 可用于共鸣
- 仅主页
- 仅自己

不每次弹窗。

### 验收任务
1. 单手从表达主页开始写一句话
2. 加一张图片
3. 改成“仅主页”
4. 发布
5. 页面稳定返回
6. 全过程无重复提示、无跳布局、无多余确认

## 5. 母版B：共鸣首页
### 目标
让用户看到“少量、真的有意思的人”，而不是刷推荐流。

### 页面性质
- 不是feed
- 不是无限刷人
- 一个人只占一个位置
- 当前可展示人数由用户设置，可少量也可不限
- 没有“下一批/刷新更多”

### 一张人物共鸣卡必须让人明显感到“这个人和另一个人不一样”
必须包含：
- 头像（头像是进入TA主页的明确入口）
- 昵称
- 当前最有代表性的一处共鸣
- TA的一句真实原话
- 我的一句真实原话
- 若已有其他共鸣脉络，用一句很轻的提示表现

不能只剩一句系统总结，否则所有人会模板化。

### 点击逻辑
- 点头像/昵称 -> TA主页
- 点共鸣正文 -> 我和TA的共鸣详情（本轮可做轻量占位，但交互必须真实）
- 不把整张卡默认绑成“进主页”

### 聊天状态
卡片可以显示“聊过”等事实，但不能用它决定是否保留TA。
共鸣和聊天互不推导。

### 原型数据至少覆盖
用6个风格和关系状态明显不同的示例人物，覆盖：
- 单次强共鸣
- 多次稳定共鸣
- 已聊过但仍有共鸣
- 有共鸣但TA关闭新聊天
- 发得很少但表达信息量高
- “有意思的不同”而非完全相似

注意：6个只是设计验收数据，不代表正式产品固定推6个人。

### 密度目标
正常手机首屏应至少能完整看到2个人，并自然露出下一位的一部分或信息起点；不能一整屏只有一个人，也不能压成密密麻麻的小列表。
具体高度由实际布局决定，不预设死数值。

## 6. 母版C：TA个人主页
### 目标
让用户既能看到TA如何主动展示自己，又能通过真实表达慢慢认识TA。

### 第一屏
必须有人感，不是“系统分析页”。
包含：
- 背景图
- 头像
- 昵称
- 本人写的简介
- 聊聊 / 继续聊（只有TA允许时才显示；TA关闭新聊天时不显示）

### 后续内容
- 置顶表达（视觉骨架与普通表达一致，只加轻量“置顶”标识）
- 最近表达
- 查看全部表达
- 我们最近/持续的共鸣入口

共鸣入口必须动态更新，不永久绑在“第一次因为这里看见TA”。

### 表达卡统一骨架
统一同宽、同字号层级、同圆角、同间距。

纯文字：正文占完整内容区。
有媒体：文字仍是主体，媒体进入固定媒体槽。
支持：
- 图片
- 音乐封面
- 电影/游戏/书封面

不能做成硬左右50/50商品列表。

### 聊天逻辑
- 不要求看完主页才能聊
- TA允许聊天时，入口随时可达
- TA不允许聊天时，主页其余内容完全正常

## 7. 这轮暂时不要做的事
- 不做完整聊天页
- 不做完整“我”页
- 不做完整自我发现页
- 不做完整共鸣主题详情
- 不做设置中心
- 不接后端
- 不接AI
- 不做登录注册
- 不做原生App
- 不打APK

## 8. 视觉质量验收
这轮不能只追求“比上一版好看”，必须同时满足：

### 视觉
- 3页明显属于同一个产品
- 页面密度稳定
- 卡片尺寸体系稳定
- 背景、头像、媒体内容出现后仍然克制和谐
- 不幼稚、不模板化、不像组件库拼装

### 易用
- 高频动作单手可达
- 点击对象和结果可预测
- 不需要猜哪里能点
- 不重复同一信息
- 不出现“必须先读完才能继续”的结构
- 键盘、返回、滚动状态稳定

### 舒服
- 连续看5分钟没有明显文字压迫感
- 没有被催促感
- 没有推荐流/刷人的兴奋机制
- 页面既不空洞，也不拥挤
- 用户随时可以停，不会觉得还有任务没完成

## 9. 输出要求
请基于用户上传的当前完整HTML继续制作，但不要机械补丁式添加。
允许重构这3个母版相关的HTML/CSS/JS，只要不破坏已确认的整体视觉方向和产品逻辑。

最终只交付：
- 一个完整可运行单文件HTML
- 3个核心母版真实可交互
- 使用真实感示例数据
- 不附设计说明，不附截图，不附长解释

交付前自行检查：
- 360×640
- 393×852
- 412×915
- 480×900
- 输入、键盘、返回、滚动、点击、状态切换
- 无JS报错