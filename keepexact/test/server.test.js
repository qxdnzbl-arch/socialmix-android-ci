import test from 'node:test';import assert from 'node:assert/strict';import{buildAuditPrompt,validateAuditBody,auditSchema}from'../server.js';const pngData='data:image/png;base64,iVBORw0KGgo=';test('audit prompt labels before/after and preserves instruction',()=>{const p=buildAuditPrompt('Remove the cup only.');assert.match(p,/BEFORE/);assert.match(p,/AFTER/);assert.match(p,/Remove the cup only/);assert.match(p,/unintended/i)});test('validation rejects missing instruction',()=>assert.equal(validateAuditBody({}).error,'Enter the exact edit instruction you gave the AI.'));test('validation rejects missing original after instruction exists',()=>assert.equal(validateAuditBody({instruction:'x'}).error,'Upload the original image.'));test('validation rejects unsupported image format',()=>assert.match(validateAuditBody({instruction:'x',original:'data:image/gif;base64,R0lGODlhAQABAIAAAAUEBA=='}).error,/^Original:/));test('validation accepts complete normalized request',()=>{const r=validateAuditBody({instruction:'  change shirt to white  ',original:pngData,edited:pngData});assert.equal(r.instruction,'change shirt to white');assert.equal(r.original.mime,'image/png');assert.equal(r.edited.mime,'image/png')});test('schema requires all top-level result fields',()=>{assert.deepEqual(auditSchema.required,['verdict','score','summary','requested_changes','unintended_changes','repair_prompt']);assert.equal(auditSchema.additionalProperties,false)});test('running server serves home, health, demand signal, and validates API without a key',async t=>{const{app}=await import('../server.js');await new Promise(resolve=>app.listen(0,'127.0.0.1',resolve));t.after(()=>new Promise(resolve=>app.close(resolve)));const base=`http://127.0.0.1:${app.address().port}`;const home=await fetch(`${base}/`);assert.equal(home.status,200);const html=await home.text();assert.match(html,/Did the AI change anything/);assert.match(html,/I’d pay ¥9 for 30 checks/);const health=await fetch(`${base}/health`);assert.deepEqual(await health.json(),{ok:true,service:'keepexact',verifier:'deepseek-flash',live:false});const interest=await fetch(`${base}/api/interest`,{method:'POST'});assert.equal(interest.status,200);assert.equal((await interest.json()).ok,true);const missing=await fetch(`${base}/api/audit`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({})});assert.equal(missing.status,400);assert.equal((await missing.json()).error,'Enter the exact edit instruction you gave the AI.');const noKey=await fetch(`${base}/api/audit`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({instruction:'change shirt',original:pngData,edited:pngData})});assert.equal(noKey.status,503)});