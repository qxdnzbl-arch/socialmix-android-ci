const MAX_BYTES=10*1024*1024;const allowedTypes=new Set(['image/jpeg','image/png','image/webp']);const form=document.querySelector('#audit-form'),submit=document.querySelector('#submit'),errorBox=document.querySelector('#form-error'),result=document.querySelector('#result'),instruction=document.querySelector('#instruction'),liveStatus=document.querySelector('#live-status');function setError(message=''){errorBox.textContent=message;errorBox.hidden=!message}async function checkLive(){try{const r=await fetch('/health',{cache:'no-store'});const d=await r.json();if(!d.live){form.querySelectorAll('input,textarea,button[type="submit"]').forEach(el=>el.disabled=true);liveStatus.textContent='Live checks are not enabled yet. You can still see the sample above and register paid interest without any charge.'}else{liveStatus.textContent='Live AI checking is enabled.'}}catch{liveStatus.textContent='Live verifier status is temporarily unavailable.'}}checkLive();function setupFileInput(id){const input=document.querySelector(`#${id}`),preview=document.querySelector(`#${id}-preview`),name=document.querySelector(`#${id}-name`);let objectUrl=null;input.addEventListener('change',()=>{setError();const file=input.files?.[0];if(!file)return;if(!allowedTypes.has(file.type)){input.value='';return setError('Use a JPG, PNG, or WEBP image.')}if(file.size>MAX_BYTES){input.value='';return setError('Each image must be 10 MB or smaller.')}if(objectUrl)URL.revokeObjectURL(objectUrl);objectUrl=URL.createObjectURL(file);preview.src=objectUrl;preview.hidden=false;name.textContent=file.name})}setupFileInput('original');setupFileInput('edited');function findingHtml(title,detail){const item=document.createElement('div');item.className='finding';const strong=document.createElement('strong');strong.textContent=title;const p=document.createElement('p');p.textContent=detail;item.append(strong,p);return item}function renderResult(data){const verdict=document.querySelector('#verdict');verdict.textContent=data.verdict;verdict.className=`verdict ${data.verdict}`;document.querySelector('#score').textContent=data.score;document.querySelector('#summary').textContent=data.summary;const requested=document.querySelector('#requested-list');requested.replaceChildren();for(const item of data.requested_changes||[])requested.append(findingHtml(`${item.status.replaceAll('_',' ').toUpperCase()} · ${item.item}`,item.evidence));if(!data.requested_changes?.length){const empty=findingHtml('No requested changes identified','The verifier could not split the instruction into a concrete requested edit.');empty.classList.add('empty');requested.append(empty)}const unintended=document.querySelector('#unintended-list');unintended.replaceChildren();for(const item of data.unintended_changes||[])unintended.append(findingHtml(`${item.severity.toUpperCase()} · ${item.area}`,`${item.change} ${item.why_it_matters}`));if(!data.unintended_changes?.length){const empty=findingHtml('No material unintended change detected','KeepExact did not identify an unrelated visible change with enough confidence to flag it.');empty.classList.add('empty');unintended.append(empty)}document.querySelector('#repair-prompt').textContent=data.repair_prompt;result.hidden=false;result.scrollIntoView({behavior:'smooth',block:'start'})}function readAsDataUrl(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=()=>reject(new Error('Could not read the selected image.'));reader.readAsDataURL(file)})}form.addEventListener('submit',async event=>{event.preventDefault();setError();const original=document.querySelector('#original').files?.[0],edited=document.querySelector('#edited').files?.[0],text=instruction.value.trim();if(!original)return setError('Upload the original image.');if(!edited)return setError('Upload the edited image.');if(!text)return setError('Enter the exact edit instruction you gave the AI.');submit.disabled=true;submit.textContent='Checking…';result.hidden=true;try{const[originalData,editedData]=await Promise.all([readAsDataUrl(original),readAsDataUrl(edited)]);const response=await fetch('/api/audit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({original:originalData,edited:editedData,instruction:text})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Verification failed.');renderResult(data)}catch(error){setError(error.message||'Verification failed.')}finally{submit.disabled=false;submit.textContent='Check edit'}});document.querySelector('#copy-repair').addEventListener('click',async event=>{const text=document.querySelector('#repair-prompt').textContent;if(!text)return;await navigator.clipboard.writeText(text);const button=event.currentTarget,old=button.textContent;button.textContent='Copied';setTimeout(()=>{button.textContent=old},1200)});const interestButton=document.querySelector('#buy-intent');interestButton.addEventListener('click',async()=>{interestButton.disabled=true;interestButton.textContent='Recording…';try{const r=await fetch('/api/interest',{method:'POST'});if(!r.ok)throw new Error();document.querySelector('#intent-status').textContent='Recorded. No payment was taken.';interestButton.textContent='Interest recorded'}catch{document.querySelector('#intent-status').textContent='Could not record this click. Try again.';interestButton.disabled=false;interestButton.textContent='I’d pay ¥9 for 30 checks'}});