name: Agent runtime acceptance
on:
  push:
    branches: [always-on-agent-v0]
    paths: ['always-on-agent/**', '.github/workflows/agent-budget-fuse.yml']
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r always-on-agent/requirements.txt pytest==8.4.2
      - run: python -m compileall -q always-on-agent
      - run: python -m pytest -q always-on-agent
