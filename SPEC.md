# 可话 Original Production SPEC

Status: Phase 1 production baseline  
Product: 可话  
Goal: 正式公开运营、同一账号多端互通的原版可话核心体验。  
Source priority: 本文件 > KEHUA-REVIVAL-AUTHORITY.md > 已核实公开原版资料 > 历史代码。

## 1. 必须达成

- 正式公网可访问，不是本地原型、截图复刻或静态演示。
- Android、iPhone/iPad、Windows、macOS 使用同一生产服务与同一账号数据。
- 一级结构固定为：首页 / 消息 / 我。
- 核心闭环固定为：发布想法 → 产生共鸣 → 在点亮前只看内容、不暴露身份 → 点亮后解锁对方身份 → 私聊 → 可发好友申请 → 对方接受后成为好友。
- 同一账号在两个独立会话中必须看到同一份服务端内容。
- 账号必须支持注册、登录、退出、恢复、注销。
- 公开客户端不得直接获得业务表读写权限。
- 支持文本发布；Phase 1 支持单张图片。
- 支持举报、屏蔽。
- Android 安装包必须通过构建、启动、公开生产页面加载和危险权限检查后才交付。

## 2. 本阶段明确假设

- 原版当前公开资料不足以核实历史账号体系的完整细节，因此 Phase 1 使用“账号 + 密码 + 一次性恢复码”。这属于运行基础设施，不改变可话核心社交机制。
- iPhone/iPad/Windows/macOS 以同一公网 PWA 作为 Phase 1 客户端；Android 同时提供 PWA 与安装 APK。所有端共享同一 Supabase 生产后端。
- 原版逐像素视觉只能以已核实的官方页面、官方截图与用户确认结构为依据；未被证据覆盖的像素细节不宣称 1:1。
- 不把旧 SocialMix、旧碎碎念、静态截图版视为本产品组成部分。

## 3. 产品行为

### 首页
- 页面出现日期、“此刻，说你想说的话～”、大块白色输入区域。
- 用户可输入 1–1200 字；可附单张图片；可设仅自己可见。
- 发布后数据落到生产后端。
- 非私密内容会与其他真实用户内容形成共鸣候选。
- 有新共鸣时显示“共鸣已到达。请签收～！”。

### 共鸣
- 打开共鸣时首先只显示内容，不显示作者身份。
- 用户可划过/下一条，或点亮。
- 只有点亮后才返回并显示作者身份。
- 点亮后双方获得可私聊关系。

### 消息
- 展示可聊天对象、好友、收到的点亮与好友申请。
- 聊天消息写入同一生产数据库；另一账号会话能读取到。
- 好友关系不是单方直接成立：一方发申请，另一方接受后成立。

### 我
- 展示/编辑昵称、简介、地区。
- 展示自己的发布与好友。
- 可重新生成恢复码、退出登录、永久注销账号。

## 4. 安全边界

- 密码只保存 bcrypt 哈希。
- 恢复码只保存 bcrypt 哈希；新恢复码生成后旧码失效。
- 会话使用高熵随机 UUID token，并有过期时间。
- register/login/recover 有服务端限流。
- 业务表对 anon/authenticated/public 无直接表权限；公开访问通过受控 RPC。
- 聊天/资料访问必须满足关系条件，并检查屏蔽关系。
- 客户端展示用户文本必须转义；图片只接受安全 data:image 类型。
- Android 不申请联系人、相机、录音、精确/粗略定位、短信、通话记录等危险权限。

## 5. Given / When / Then 验收

### A. 同账号跨端
Given 同一账号已注册  
When 会话 A 发布一条内容，并在独立会话 B 登录同一账号  
Then 会话 B 能从服务端读取到该内容。

### B. 真实共鸣
Given 账号 A 和账号 B 各发布一条非私密内容  
When A 刷新首页  
Then A 获得包含 B 内容的服务端共鸣记录。

### C. 点亮前隐私
Given A 有来自 B 的共鸣  
When A 尚未点亮  
Then共鸣返回体不得包含 B 的用户 ID、昵称或头像。

### D. 点亮
Given A 正查看来自 B 的共鸣  
When A 点亮  
Then 服务端返回 B 的身份资料，并建立允许双方私聊的关系。

### E. 跨账号聊天
Given A 已点亮 B  
When A 发送消息  
Then B 的独立会话可以读取该消息。

### F. 好友
Given A 与 B 已有可互动关系  
When A 发好友申请且 B 接受  
Then A 与 B 的好友列表均基于同一服务端好友关系。

### G. 恢复
Given A 保存了注册时得到的恢复码  
When A 用账号 + 恢复码设置新密码  
Then旧会话失效，新密码可用，并返回新的恢复码，旧恢复码失效。

### H. 注销
Given A 已登录  
When A 确认永久注销  
Then账号及其级联业务数据从生产账号体系删除，旧会话不可继续使用。

### I. Android 成品
Given CI 从生产分支构建 APK  
When 在 Android 35 模拟器安装并启动  
Then应用成功加载正式公网可话页面，页面标题和可见内容包含“可话”，进程保持存活，且不申请禁止的危险权限。

## 6. 完成定义

只有同时满足以下条件才可称为 Phase 1 可交付：
1. 生产数据库迁移成功。
2. 核心服务端自检 A–G 全部为 true。
3. 公网 Web/PWA Edge Function 为 ACTIVE。
4. Android APK 构建成功。
5. Android 模拟器验收成功。
6. 产出可下载 APK 和 SHA256。
7. 不把未核实的逐像素差异或商店上架状态冒充已完成。
