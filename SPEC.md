# 可话原版复刻生产 SPEC

Status: Evidence-locked rebuild
Product: 可话（按公开可核验原版界面与交互复刻，不冒充原官方客户端）
Goal: 正式公开运营、Android / iPhone-iPad / Windows / macOS 独立安装、同一账号与数据互通。

## 0. 权威顺序

1. 用户当次明确确认与纠错。
2. 已核验原版可话截图、App Store 官方描述、原版实测流程。
3. KEHUA-REVIVAL-AUTHORITY.md。
4. 当前生产后端仅作为基础设施，不得反向定义产品。

此前自创的“紫色胶囊底部导航 + 首页我说过的话信息流 + 通讯录式消息页 + 恢复码/退出/注销直接占据个人主页”的版本全部作废，不得作为视觉或交互基线。

## 1. 已核验的原版证据

- 2022 Android 1.7.6 首页实测截图：大面积背景、日期、“此刻，说你想说的话～”、大白色圆角输入卡、底部仅三个线框入口。
- 2022 实测截图：发布后首页出现白色圆角状态卡，“正在寻找共鸣，请稍等～”。
- App Store 宣传截图：一条动态本身就是聊天上下文；点亮后显示用户身份并在动态下直接交流，回复仅彼此可见。
- App Store 宣传截图：消息页顶部是好友头像横列，下方是基于动态/对话的消息列表。
- 原版实测流程图：写动态 → 动态匹配 → 查看匹配动态 → 点亮 → 开启交流权限 → 回复动态 → 查看用户信息 → 添加好友；被点亮侧收到提醒并进入动态对话列表。
- App Store 官方说明：每次记录得到几条“共鸣”；内容直达懂你的人；只有互相理解的人能看到你说的话，双方互动仅彼此可见。
- App Store 1.10.1 版本记录：将私聊和动态下的聊天消息合并进一个聊天框，说明“动态上下文聊天”是原版核心行为。

## 2. 一级结构

仅三个一级入口：
- 首页
- 消息
- 我

底部导航使用简洁线框图标；选中态只改变线条深浅，不使用上一错误版本的紫色胶囊背景。

## 3. 首页

- 日期。
- 标题固定：“此刻，说你想说的话～”。
- 大白色圆角输入卡，placeholder：“我想说...”。
- 文本动态，可附单张图片，可设仅自己可见。
- 发布后进入“正在寻找共鸣，请稍等～”状态。
- 匹配完成后显示“共鸣已到达。请签收～！”。
- 首页不展示“我说过的话”信息流；自己的历史动态归入“我”。

## 4. 共鸣与点亮

- 共鸣以逐条卡片浏览，不做公开广场。
- 点亮前以内容为中心，不主动展示用户身份。
- 支持左右滑动/下一条。
- 点亮后显示对方身份，并从这条动态直接开始交流。
- 聊天页保留原动态作为上下文；回复仅双方可见。

## 5. 消息

- 顶部标题“消息”。
- 已成为好友的人以头像横列作为快捷入口。
- 主体是“从动态/共鸣开始的对话”列表。
- 收到点亮、好友申请均形成可处理入口。
- 不做上一错误版本那种纯通讯录式空页。

## 6. 我

- 个人资料。
- “我的动态”和“点亮”两类内容。
- 设置入口单独收纳编辑资料、账号恢复、退出、注销等系统动作。
- 工程性账号恢复功能不得直接占据个人主页主视觉。

## 7. 生产数据与安全

- 四端必须使用同一 Supabase 生产项目与同一业务数据。
- 发布、共鸣、点亮、聊天、好友、举报、屏蔽、注销均服务端落库。
- 客户端仅允许 publishable key，不得包含 service_role、数据库密码、Apple 私钥或 Windows 签名私钥。
- 用户文本输出必须转义；图片仅接受安全 data:image 类型。
- 同账号内容与互动必须能跨设备读取。

## 8. 登录现状与正式门槛

历史原版资料支持手机号验证码登录。当前生产后端仍是“账号 + 密码 + 恢复码”；这只能作为当前可运行基础设施，不得被宣称为已复刻的原版账号体系。

正式公开运营前必须切换为真实可用的手机号验证码流程，并配置生产短信供应商。没有真实 SMS 供应商配置时，不得伪造“验证码登录已完成”。

## 9. 多端独立安装

- Android：APK / AAB。
- iOS：原生 iOS 应用工程并生成可签名 Archive；正式 App Store IPA 必须使用用户自己的 Apple Developer / Distribution / App Store Connect 身份。
- macOS：独立 .app / DMG；正式公开分发需 Developer ID 签名与 Apple notarization。
- Windows：独立 .exe 安装器；正式公开分发需受信任代码签名，避免未签名安装器的系统警告。
- 不以 PWA 代替任何一个独立客户端。

## 10. 场景验收

### Scenario A — 原版首页
Given 已登录  
When 打开首页  
Then 可见日期、“此刻，说你想说的话～”、大白色输入卡和三个线框底部入口；首页不存在“我说过的话”信息流，也不存在紫色胶囊选中导航。

### Scenario B — 发布与共鸣
Given 用户写下一条公开动态  
When 发布  
Then 服务端保存该动态，进入寻找共鸣状态；匹配完成后出现“共鸣已到达。请签收～！”。

### Scenario C — 内容先于身份
Given 用户打开一条未点亮共鸣  
When 浏览  
Then 先看到动态内容；点亮后才进入带身份的互动上下文。

### Scenario D — 动态上下文交流
Given 用户已经点亮一条动态  
When 发送回复  
Then 聊天页保留触发交流的动态上下文，另一账号的独立会话能读取消息。

### Scenario E — 消息结构
Given 已有好友、点亮或动态对话  
When 打开消息  
Then 顶部好友头像横列、下方动态/对话列表与好友申请入口均可用。

### Scenario F — 个人页
Given 用户已有自己发布和点亮的动态  
When 打开“我”  
Then 可以在“我的动态 / 点亮”间切换；设置动作不挤占主页主视觉。

### Scenario G — 跨端互通
Given 同一账号在两种独立客户端登录  
When A 端发布、点亮或聊天  
Then B 端读取同一生产服务端状态。

### Scenario H — 独立安装件
Given CI 从同一提交构建  
When 分别构建 Android、Windows、macOS、iOS  
Then 得到各自平台的独立安装/归档产物，并连接同一生产后端。

## 11. 最终完成定义

只有以下全部满足才可称“正式公开运营多端原版可话”：
1. 原版证据覆盖的界面与交互已实现并经过真实截图/场景验收。
2. 用户最终主观确认界面与交互没有再偏离原版。
3. 真实手机号验证码生产链路完成。
4. Android 安装与启动验收通过。
5. Windows 安装、启动验收通过，正式发行件完成受信任签名。
6. macOS 安装、启动验收通过，正式发行件完成 Developer ID 签名和 notarization。
7. iOS 使用用户 Apple Developer 身份签名并上传 App Store Connect，满足 UGC 社交审核要求并通过审核。
8. 四端账号/数据互通经过实际跨端测试。
9. 最终只交付真实安装件、SHA256、构建提交和验收记录；任何候选件不得冒充最终正式发行版。
