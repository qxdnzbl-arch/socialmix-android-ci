# SocialMix Android CI

Public build and acceptance-test repository for SocialMix Phase 1. No private credentials are stored here.

CI runs an Android emulator smoke test before publishing the APK artifact.
