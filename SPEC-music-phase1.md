# SPEC — 沉浸音乐 Phase 1

## One-Sentence Concept
一个只负责“听歌、找歌、管理自己喜欢歌曲”的极简 Android 音乐播放器：保留网易云音乐当前播放器中用户明确喜欢的沉浸感和关键交互，删掉不需要的功能，并让所有操作尽量少一层、轻量、跟手。

## Background / Motivation
用户喜欢网易云音乐当前播放器的沉浸式黑胶、纯色背景和整体视觉，但不需要复杂推荐、社区、笔记、会员运营等功能。用户只会把真正喜欢的手机歌曲导入 App，因此“音乐库”本身就代表用户保留的喜欢歌曲，不需要再重复出现“我喜欢的音乐”或“本地音乐”说明。

本轮重点不是继续加功能，而是把已经存在的操作做得更直接：删除不再经过“歌曲选项”二级底部面板；图标统一；搜索页缩回整体比例；去掉图标按下时的圆形 Material 波纹。

## Target User And Use Scenario
目标用户：当前提出需求的单人用户。

使用场景：
- 打开 App 直接听歌。
- 在首页快速切歌、切换顺序播放/单曲循环、打开播放列表。
- 在播放列表临时移除不想继续播放的歌曲。
- 在音乐库查看自己已经导入并保留的歌曲，并按需从 App 音乐库删除。
- 需要时用搜索快速找到已有歌曲。

## Core User Path
打开 App → 沉浸首页播放 → 播放/暂停/上一首/下一首/拖动进度 → 顺序播放或单曲循环 → 打开播放列表 → 长按歌曲后该行直接出现“删除” → 删除只影响当前队列 → 进入音乐库直接看到歌曲列表 → 点歌曲右侧竖向三点后该行直接出现“删除” → 删除则从 App 音乐库和当前队列移除 → 搜索已有歌曲。

## Platform & Technical Constraints
- Android 原生 App，Jetpack Compose。
- 用户提供的网易云真机截图是保留区域的视觉母版。
- 不伪造网易云官方内部设计参数；公开无法取得的参数以用户真机截图比例为最高依据。
- Phase 1 不接商业在线曲库。
- 手机音乐通过 Android MediaStore 扫描，并在 App 内选择导入。
- 已导入手机歌曲需要持久保存。
- 播放队列与 App 音乐库是两个独立数据层。
- 两类删除都不删除手机原始音乐文件。
- 所有主要图标点击保留可点击语义和命中区域，但不显示圆形/矩形 Material 波纹。

## Phase 1 Scope

### 首页播放器
- 打开即进入沉浸播放器。
- 背景为当前歌曲对应的深色纯色，切歌时平滑换色。
- 黑胶主体接近真实黑色，沟槽细密，高光克制，不出现明显灰色月牙块。
- 播放时黑胶旋转，暂停时停止。
- 唱针暂停时离盘，播放时落盘。
- 顶部只保留“心动”和搜索；“心动”下不显示横杠。
- 显示歌名、歌手、进度、时间、极高音质、播放模式、播放列表。
- 原爱心位置改为播放模式按钮。
- 默认“顺序播放”；点击切到“单曲循环”；再次点击回到“顺序播放”。
- 播放模式图标必须简洁、轻、与右侧播放列表图标大小和视觉重量一致。
- 提供上一首、播放/暂停、下一首。
- 中间播放三角和左右切歌图标使用圆润轮廓，不使用尖锐 Material 默认三角。
- 进度圆点保持小尺寸；时间字体轻、细、窄。
- 底部一级导航只有“首页 / 音乐库”，两页位置完全一致。
- 首页所有可点击图标不显示圆形透明波纹。

### 播放列表与队列删除
- 播放列表展示当前播放队列，不等于音乐库。
- 长按某一首歌曲后，不打开新的“歌曲选项”底部面板。
- 长按后直接在该歌曲行右侧显示“删除”。
- 点击该行“删除”只从当前播放队列移除歌曲。
- 该歌曲仍保留在音乐库中，也不删除手机原文件。
- 之后从音乐库或搜索重新点歌时可以再次加入播放队列。
- 如果删除当前播放歌曲，按当前播放状态顺延到下一首；队列为空时安全停止。

### 音乐库与音乐库删除
- 页面顶部只显示“音乐库”和右上角添加音乐入口。
- 不显示“最近播放”。
- 不显示“本地音乐”。
- 不显示“我喜欢的音乐”标题、卡片、数量或其他重复说明。
- 音乐库标题下直接显示歌曲列表。
- 歌曲右侧使用竖向三点 `⋮`，不使用横向三个点。
- 点击竖向三点后，不打开新的“歌曲选项”底部面板。
- 点击三点后直接在该歌曲行右侧显示“删除”。
- 点击该行“删除”后，从 App 音乐库持久移除，同时从当前播放队列和 App 导入记录移除。
- 不删除手机原始音乐文件。
- 音乐库底部不显示迷你播放器。
- 页面图标点击不显示圆形透明波纹。

### 搜索
- 搜索只是工具入口，不是底部主导航。
- 搜索页整体比例必须与首页/音乐库一致，不像单独的一套大尺寸界面。
- 返回图标、搜索框高度、搜索图标、占位文字、顶部留白都比旧版收小。
- 支持按已有歌曲的歌名、歌手搜索。
- 不显示榜单、信息流、运营推荐或社区内容。
- 搜索页图标点击不显示圆形透明波纹。

### 手机音乐导入
- 右上角添加音乐入口保留。
- 未授权时请求系统音频读取权限。
- 授权后在 App 内打开“选择手机音乐”面板，不跳系统文件夹。
- 导入后的歌曲直接进入音乐库。
- 已导入歌曲可以播放、搜索，并在重启后继续存在。

## Explicitly Out Of Scope
- 在线曲库接入。
- 网易云账号、QQ 音乐账号、Apple Music 账号接入。
- 删除手机物理音频文件。
- “最近播放”区块和相关删除逻辑。
- “我喜欢的音乐”额外页面/标题/卡片。
- “本地音乐”独立分类。
- 随机播放。
- 推荐 Tab、笔记、动态、关注、粉丝、播客、直播、K 歌、短视频、商城、游戏、装扮、等级。
- VIP、广告、活动入口、运营弹窗。
- 评论、社交分享、私信。
- 为了“显得完整”重新增加中间确认页、二级操作页或复杂菜单。

## Rabbit Holes / Scope Risks
- 不把两个删除合并：队列删除只删队列，音乐库删除才做持久删除。
- 不为了防误触重新加“歌曲选项”二级面板；本轮明确要求少一层。
- 不把音乐库重新拆成“喜欢/本地/最近”等重复概念。
- 不用不同来源的图标混搭造成视觉重量不一致。
- 不为了搜索功能增加第三个底部导航。

## Assumptions
- 用户导入到这个 App 的歌曲本身就代表“这是我想保留的音乐”。
- 音乐库本身已经表达“我保留的歌曲”，无需“我喜欢的音乐”重复标题。
- 删除手机原文件属于不可逆高风险操作，本轮明确不做。
- 用户提供的网易云截图优先于通用 Material 默认视觉。

## Scenario Acceptance Tests

### Scenario 1：首页基础视觉与状态
Given App 打开且暂停
When 查看首页
Then 背景为深色纯色；黑胶接近黑色并有细沟槽；唱针离盘；播放模式和播放列表图标尺寸/视觉重量协调。

### Scenario 2：播放模式
Given 首页显示“顺序播放”
When 点击播放模式一次，再点击一次
Then 第一次变为“单曲循环”，第二次恢复“顺序播放”；图标简洁且状态同步。

### Scenario 3：播放列表直接删除
Given “First Light”在当前播放队列中
When 打开播放列表并长按“First Light”
Then 不出现“歌曲选项”底部面板；同一歌曲行直接出现“删除”。
When 点击该“删除”
Then “First Light”只从当前队列移除，App 不崩溃。

### Scenario 4：音乐库不再重复说明
Given 用户进入音乐库
When 查看页面
Then 不存在“最近播放”“本地音乐”“我喜欢的音乐”标题/卡片；音乐库标题下直接是歌曲列表或空状态。

### Scenario 5：音乐库直接删除
Given 音乐库中存在一首导入歌曲
When 点击该歌曲右侧竖向三点
Then 不出现“歌曲选项”底部面板；同一行直接显示“删除”。
When 点击“删除”
Then 歌曲从 App 音乐库和当前队列移除，但手机原文件不删除。

### Scenario 6：搜索比例与功能
Given 用户从首页进入搜索
When 查看搜索页
Then 返回图标、搜索框、文字和顶部留白明显收小并与整体 App 比例一致；仍可搜索已有歌曲。

### Scenario 7：无图标圆形点击波纹
Given 用户在首页、音乐库或搜索页
When 点击搜索、播放模式、播放列表、上一首/下一首、播放/暂停、导入、更多、返回等图标
Then 不出现 Material 圆形/矩形透明点击波纹，点击仍正常生效。

### Scenario 8：手机音乐在 App 内选择
Given App 已获得音频读取权限
When 点击添加音乐
Then 在 App 内显示“选择手机音乐”面板，不进入系统文件夹界面。

## Launch / Deliverable Checklist
- Gradle Debug APK 构建成功。
- Android 35 模拟器完成场景验收。
- 真实截图至少覆盖：首页暂停、首页播放、音乐库。
- 人工复检：音乐库无重复“我喜欢的音乐”标题；三点为竖向；搜索页比例收小；播放模式图标与播放列表统一；无多余“歌曲选项”二级面板路径。
- APK、SPEC、SHA256 一起打包交付。

## Handoff Prompt For Development
请根据这份 SPEC 实现本轮 Phase 1 迭代。只做 Phase 1，不增加 Explicitly Out Of Scope 内容。完成后按 Scenario Acceptance Tests 自测；修复所有失败项后再交付 APK。
