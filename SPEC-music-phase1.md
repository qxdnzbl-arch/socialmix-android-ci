# SPEC — 沉浸音乐 Phase 1

## One-Sentence Concept
一个只负责“听歌、找歌、管理自己喜欢歌曲”的极简 Android 音乐播放器：保留网易云音乐当前播放器中用户明确喜欢的沉浸感和关键交互，删掉不需要的功能，并让所有操作尽量少一层、轻量、跟手。

## Background / Motivation
用户喜欢网易云音乐当前播放器的沉浸式黑胶、纯色背景和整体视觉，但不需要复杂推荐、社区、笔记、会员运营等功能。用户只会把真正喜欢的手机歌曲导入 App，因此“音乐库”本身就代表用户保留的喜欢歌曲，不再重复出现“我喜欢的音乐”“本地音乐”“最近播放”等说明。

当前迭代继续收敛已有体验：播放页长歌名和长歌手名都改为高质量、单方向、完整循环 marquee；播放模式图标改成更专业的单线圆形箭头；首页、音乐库、搜索页统一顶部基准线、页面边距、标题字号和图标规格；搜索页删除“只搜索你的歌曲”这条多余说明。

## Target User And Use Scenario
目标用户：当前提出需求的单人用户。

主要场景：打开 App 直接听歌；在首页切歌、切换顺序播放/单曲循环、打开播放列表；在播放列表临时移除歌曲；在音乐库查看导入歌曲并按需从 App 音乐库删除；需要时快速搜索已有歌曲。

## Core User Path
打开 App → 首页沉浸播放 → 播放/暂停/上一首/下一首/拖动进度 → 顺序播放或单曲循环 → 打开播放列表 → 长按歌曲时该行中央浮现“删除” → 需要找歌时进入紧凑搜索页 → 进入音乐库查看导入歌曲 → 点竖向三点时该行中央浮现“删除” → 右上角继续添加手机歌曲。

## Platform & Technical Constraints
- Android 原生 App，Jetpack Compose。
- 用户提供的网易云真机截图是保留区域的视觉母版；找不到公开官方设计参数时不得编造参数，直接按截图比例校准。
- Phase 1 只使用演示音轨和手机 MediaStore 本地歌曲，不接商业在线曲库。
- 播放队列与 App 音乐库是两个独立数据层。
- 播放列表删除只删除当前队列项；音乐库删除会从 App 音乐库及当前队列移除，但绝不删除手机原始音频文件。
- 已导入歌曲需要持久保存。
- 同一层级页面不得各自维护一套随意尺寸；一级页面顶部必须使用同一组视觉基准值。

## Phase 1 Scope

### 全局视觉系统
- 首页、音乐库、搜索页使用统一的顶部基准：状态栏后统一顶部间距、统一 52dp 顶部功能区高度。
- 一级页面左右主边距统一为 22dp。
- 首页“心动”和音乐库“音乐库”使用同一页面标题字号和字重。
- 一级页面顶部图标统一 44dp 点击区、22dp 可见尺寸；图标点击不显示明显圆形 Material 波纹。
- 底部“首页 / 音乐库”继续使用同一个共享组件，切页时几何位置、字号、选中状态不跳动。
- 同类歌曲列表使用同一歌名/歌手字号与层级，不因为页面不同随意变化。

### 首页播放器
- 背景是更深、更饱和的歌曲主题纯色，不使用封面模糊背景、雾化渐变或多余光效。
- 黑胶主体接近真实黑色，保留细密沟槽和克制反光，不出现明显灰色月牙高光块。
- 暂停时唱针离盘，播放时唱针落盘。
- 顶部只保留“心动”和搜索，“心动”无下划线。
- 长歌名禁止固定显示 `...`：超出可用宽度时，先显示开头，随后以单一方向、恒定速度完整滚过全文，结尾经过后再进入下一轮；禁止左右来回弹、禁止半截文本、禁止逐帧重新布局导致文字抖动。
- 长歌手名使用与歌名相同的单向完整循环规则；短歌名/短歌手名不滚动。
- marquee 使用 Compose 原生绘制型滚动能力，避免手写逐帧 translation 引起抖动。
- 歌名使用较柔和的白色，歌手进一步降低亮度；字号、字重、行高保持轻、松、大气。
- 歌名/歌手区域与右侧“播放模式 / 播放列表”重新分配空间：左侧文字区域占剩余宽度，右侧两个图标固定同一视觉尺寸、线重和垂直中心，文字与图标之间留出明确间距。
- 播放模式只保留顺序播放与单曲循环。图标使用一条圆形连续线和一个干净箭头头部，不出现交叉的小叉或分支；单曲循环只在同一图形中增加小号 `1`。
- 播放模式图标和播放列表图标视觉重量一致。
- 上一首/下一首和中心播放图标继续使用圆润自定义轮廓，点击无明显波纹。
- 进度圆点保持小尺寸；时间字体保持更细、更轻。

### 播放列表与删除
- 播放列表展示当前播放队列。
- 长按某首歌曲后，不打开“歌曲选项”底部面板，也不把“删除”放到最右侧。
- 删除操作直接在当前歌曲行中央浮现，原歌曲内容弱化但仍能看出上下文。
- 用户点中央“删除”后，只从当前播放队列移除该歌曲，音乐库和手机原文件不受影响。
- 用户不想删除时，点击当前行其他位置取消删除状态，不触发播放。
- 删除当前播放歌曲时按当前状态顺延；队列删空时停止，不崩溃。

### 音乐库
- 页面只显示标题“音乐库”和歌曲列表，不再显示“我喜欢的音乐”“本地音乐”“最近播放”。
- 右上角保留添加手机音乐入口。
- 每首歌右侧使用竖向三点 `⋮`，而不是横向三点。
- 点击 `⋮` 后，不弹独立歌曲选项面板；“删除”直接在当前歌曲行中央浮现，不要求手指再移动到右边。
- 点击中央“删除”后，从 App 音乐库以及当前播放队列移除该歌曲，但不删除手机原文件。
- 音乐库底部不显示迷你播放器。

### 搜索
- 搜索只是首页工具入口，不做底部主导航。
- 只搜索演示音轨和已导入歌曲。
- 搜索栏使用自定义紧凑输入结构，不使用通过硬压 Material TextField 高度造成的裁字方案。
- 返回箭头、搜索图标、输入文字、占位文字全部垂直居中；“搜索歌曲或歌手”必须完整显示，不能被上下裁切。
- 搜索顶部区域使用与首页、音乐库相同的顶部间距和 52dp 功能区高度。
- 搜索页完全删除“只搜索你的歌曲”，不留占位空行；空查询时搜索框下方直接为空。

### 手机音乐导入
- 通过 Android MediaStore 在 App 内展示“选择手机音乐”面板，不跳系统文件夹。
- 导入即进入音乐库。
- 已导入歌曲可搜索、播放，并在重启后继续存在。

## Explicitly Out Of Scope
- 网易云、QQ 音乐、Apple Music 等在线曲库接入。
- 账号、支付、广告、VIP、社区、笔记、动态、推荐信息流、直播、K 歌、短视频。
- 随机播放。
- 删除手机物理音频文件。
- 重新增加“最近播放”“本地音乐”“我喜欢的音乐”重复分区。
- 为删除操作增加确认页、歌曲选项页或额外二级弹窗。
- 为了视觉效果擅自新增玻璃卡片、渐变底板或复杂动效。
- 为长文本恢复左右往返式滚动。

## Rabbit Holes / Scope Risks
- 不把当前视觉迭代扩展成新功能开发。
- 不把播放队列删除和音乐库删除合并成同一种数据操作。
- 不用默认 Material Repeat 图标冒充用户要求的单线极简播放模式图标。
- 不通过简单缩小 TextField 高度再次制造文字裁切。
- 不用静态省略号代替长歌名/长歌手名完整展示。
- 不再用手写每帧位移动画制造文字抖动。

## Assumptions
- 用户提供的网易云截图是当前视觉验收最高依据。
- “完整循环”按单方向 marquee 实现：展示开头 → 匀速经过全文 → 结尾完整经过 → 下一轮；不是左右来回移动。
- 两类删除都不触碰手机原始音乐文件。
- 当前只验证手机本地曲库体验；合法在线曲库属于后续阶段。

## Scenario Acceptance Tests

### Scenario 1：首页标题和歌手完整循环
Given 首页存在超出文字区域的长歌名或长歌手名
When 用户停留在播放页
Then 两者都只向一个方向完整循环；能看到从开头到最后字符，不反向、不固定省略、不抖动；短文本保持静止。

### Scenario 2：播放模式图标
Given 默认处于顺序播放
When 用户连续点击播放模式图标两次
Then 第一次变为单曲循环并显示小号 `1`，第二次恢复顺序播放；图形只有一条圆形连续线和一个干净箭头头部，不出现小叉或分支，视觉尺寸与右侧播放列表一致。

### Scenario 3：播放列表原地删除
Given 播放列表中存在 First Light
When 用户长按该歌曲
Then 不出现“歌曲选项”独立面板，当前歌曲行中央直接浮现“删除”；点击删除后只从队列消失，App 不崩溃。

### Scenario 4：音乐库结构
Given 用户进入音乐库
When 查看页面
Then 不存在“最近播放”“本地音乐”“我喜欢的音乐”；歌曲右侧为竖向三点；点击三点后删除在当前行中央浮现，不打开独立歌曲选项面板。

### Scenario 5：一级页面视觉基准
Given 用户在首页、音乐库、搜索页之间切换
When 观察三个页面最顶部功能区
Then 顶部起始位置和功能区高度一致；首页“心动”和音乐库“音乐库”字号一致；顶部图标点击区/可见尺寸遵循同一规格，切页没有明显上下跳动。

### Scenario 6：搜索比例和文案
Given 用户从首页进入搜索
When 页面首次显示
Then “搜索歌曲或歌手”完整可见且垂直居中；搜索框与整体 App 比例协调；页面中不存在“只搜索你的歌曲”。

### Scenario 7：手机音乐导入
Given App 已获得音频读取权限
When 用户点击右上角添加音乐
Then 在 App 内显示“选择手机音乐”面板，不跳系统文件夹，App 不崩溃。

## Launch / Deliverable Checklist
- Gradle Debug APK 构建成功。
- Android 35 模拟器完成场景验收。
- 截图至少覆盖：首页暂停、首页播放、播放列表、音乐库、搜索页。
- 人工复检：标题/歌手滚动方向与稳定性、播放模式图标、三个页面顶部对齐、搜索页无多余提示、删除位置。
- APK 使用新的独立交付文件名，避免与旧版本混淆。

## Handoff Prompt For Development
请根据这份 SPEC 实现当前 Phase 1。只做 Phase 1，不要做 Explicitly Out Of Scope 里的内容。完成后按 Scenario Acceptance Tests 自测；只有构建、自动验收和最终截图复检都通过后，才交付 APK。