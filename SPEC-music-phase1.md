# SPEC — 沉浸音乐 Phase 1

## One-Sentence Concept
一个只负责“听歌、找歌、找回自己喜欢的歌”的极简 Android 音乐播放器：保留网易云音乐当前播放器中用户明确喜欢并要求保留的部分，并按用户提供的网易云真机截图作为视觉母版做删减式还原；不要的功能直接删除，不另起一套视觉设计。

## Background / Motivation
用户要的是网易云现有播放器的沉浸感和轻盈视觉，但不需要直播、笔记、社区、会员运营、复杂推荐页等冗余能力。当前迭代不是重新设计音乐 App，而是“保留什么就按参考母版还原什么，删除什么就彻底删除什么”。用户只会把自己喜欢的手机音乐导入 App，因此“本地音乐”和“我喜欢的音乐”不应成为两个重复概念。

## Target User And Use Scenario
目标用户：当前提出需求的单人用户。
使用场景：打开 App 就听歌；需要时快速搜索已有歌曲；进入音乐库找回自己导入的喜欢歌曲；导入手机音乐时不离开 App；需要时从当前播放列表删除歌曲。

## Core User Path
打开 App → 直接进入沉浸播放首页 → 播放/暂停/上一首/下一首/拖动进度 → 顺序播放或单曲循环 → 需要找歌时点右上角搜索 → 需要查看当前队列时打开播放列表并可长按歌曲进入删除 → 进入音乐库查看“我喜欢的音乐” → 右上角从手机选择并导入喜欢的歌曲。

## Platform & Technical Constraints
- Android 原生 App，Jetpack Compose。
- 用户提供的网易云截图是当前保留区域的视觉母版；禁止在保留区域另起视觉风格。
- Phase 1 不接网易云、Apple Music 或其他商业曲库 API。
- 自带 3 首可离线播放的演示音轨，用于验证播放、切歌、进度、唱针、背景和播放模式。
- 手机音乐通过 Android MediaStore 扫描；获得系统音频读取权限后，在 App 内展示“选择手机音乐”面板，不跳到系统文件夹界面。
- 已导入手机歌曲需要持久保存。
- “删除”默认只从本 App 的音乐库/播放列表/喜欢状态中移除，不删除手机上的原始音频文件。
- 不需要账号、支付、服务端、广告或联网曲库。

## Phase 1 Scope
### 首页播放器
- 打开即进入当前歌曲。
- 背景使用当前歌曲主题色的干净纯色；切歌时平滑换色。禁止封面模糊背景、明显渐变雾化和额外光效。纯色需比旧版更深、更饱和。
- 黑胶作为视觉中心。主体必须接近真实黑色，沟槽细密；高光应是宽而柔和的方向性反射，不允许出现明显灰色月牙块或厚重高光条；整体参考用户提供的网易云真机截图。播放时旋转，暂停时停止。
- 唱针状态与播放状态一致：暂停时离盘，播放时落盘。
- 顶部只保留“心动”和搜索；“心动”下面不显示横杠。
- 显示歌名、歌手、播放模式、播放列表、进度、当前时间/总时长、极高音质。
- 原点赞/收藏图标位置改为播放模式：默认“顺序播放”；点击后切换为“单曲循环”；再次点击恢复“顺序播放”。图标状态必须同步变化，并给出即时文字反馈。
- 顺序播放：当前歌曲结束后自动播放下一首。
- 单曲循环：当前歌曲结束后从头继续播放当前歌曲。
- 提供上一首、播放/暂停、下一首。切歌时原来播放则继续播放，原来暂停则继续暂停。
- 上一首/下一首和中心播放三角使用按网易云参考重画的圆润轮廓，禁止 Material 默认尖锐三角。
- 进度圆点按网易云母版保持小尺寸；时间字体更细、更窄、更轻。
- 底部一级导航只有“首页 / 音乐库”，两页共用完全相同的几何位置；无独立底板、无矩形点击方块。

### 播放列表与歌曲删除
- 播放列表展示当前可播放歌曲。
- 播放列表中的歌曲支持长按；长按后打开歌曲选项并提供“删除”。
- 原“从最近播放移除”文案和行为全部删除，统一改为“删除”。
- 删除后歌曲从当前 App 的音乐库、播放列表、喜欢状态中移除；若为手机导入歌曲，同时移除 App 保存的导入记录，但不删除手机原文件。

### 音乐库
- “最近播放”标题、数量、空状态和列表全部删除。
- 不再同时显示“我喜欢的音乐”卡片和“本地音乐”区。
- 音乐库只保留一个歌曲概念：“我喜欢的音乐”。用户导入手机歌曲即表示喜欢，该歌曲直接进入这一列表。
- 列表为空时提示“右上角可直接添加喜欢的音乐”。
- 右上角保留添加手机音乐入口。
- 音乐库底部不显示迷你播放器。
- 歌曲更多菜单提供“删除”，不再提供“从最近播放移除”。
- 底部“首页 / 音乐库”与首页使用同一导航几何尺寸与坐标。

### 搜索
- 搜索不是底部一级导航，只是工具入口。
- 只搜索已有歌曲：演示音轨 + 已导入手机音频。
- 支持按歌名、歌手过滤。
- 不显示榜单、信息流、运营推荐或社区内容。

### 手机音乐导入
- 点击右上角添加入口后，如未授权则请求系统音频读取权限。
- 授权后直接在 App 内打开“选择手机音乐”面板。
- 面板读取手机 MediaStore 中的音乐，显示标题、歌手、时长，并允许加入 App。
- 导入即视为“我喜欢”，不再要求用户额外点一次收藏。
- 已导入歌曲需要能被搜索、播放，并在重启后继续存在。

## Explicitly Out Of Scope
- Phase 1 不接网易云账号、网易云在线曲库或无授权版权音乐抓取。
- Phase 1 不接 Apple Music 或其他商业曲库；最终在线曲库属于后续独立阶段。
- 删除手机物理音频文件。
- 系统文件夹式导入界面。
- “最近播放”页面/区块。
- 单独“本地音乐”分类。
- 推荐 Tab、笔记、动态、关注、粉丝、播客、直播、K 歌、短视频、商城、游戏、装扮、徽章、等级。
- VIP、广告、活动入口、运营弹窗。
- 评论、社交分享、私信。
- 在线歌词、云歌单同步、下载管理。
- 为了“差不多好看”而新增用户没有要求的卡片、渐变、阴影、玻璃底板或 Material 默认视觉。

## Rabbit Holes / Scope Risks
- 不为了“像完整音乐平台”继续添加推荐、榜单、账号、云同步等功能。
- 不用伪在线曲库或无授权抓取替代真实版权能力。
- 不把搜索做成第三个主导航。
- 不把“本地音乐”和“我喜欢的音乐”重新拆成两个重复入口。
- 不把公开不可得的“网易云内部设计参数”编造成官方参数；官方参数无法取得时，直接以用户提供的网易云真机截图做比例母版。

## Assumptions
- 用户给出的网易云截图是本轮视觉验收最高依据。
- 用户导入手机歌曲本身就代表“这是我喜欢的音乐”。
- 删除只影响本 App，不删除手机文件，以避免不可逆误删。
- 最终产品目标包含合法在线曲库搜索与播放，但不属于本轮 Phase 1。

## Scenario Acceptance Tests
### Scenario 1：首页视觉与黑胶
Given App 打开并处于暂停状态
When 查看首页
Then 背景为更深的均匀主题纯色；黑胶主体接近黑色并保留细密沟槽，反射为柔和方向性高光，不出现明显灰色月牙块；暂停状态唱针离盘。

### Scenario 2：核心播放与切歌状态
Given 首页暂停
When 依次点击下一首、上一首、播放
Then 暂停切歌时中间保持播放图标且唱针离盘；播放后变暂停图标且唱针落盘；App 不崩溃。

### Scenario 3：播放模式
Given 默认显示“顺序播放”
When 点击播放模式图标一次，再点击一次
Then 第一次切成“单曲循环”，第二次恢复“顺序播放”；图标状态和文字反馈一致。

### Scenario 4：音乐库模型
Given 用户进入音乐库
When 查看页面
Then 不存在“最近播放”和“本地音乐”；只显示“我喜欢的音乐”；没有底部迷你播放器。

### Scenario 5：播放列表长按删除入口
Given 用户打开播放列表
When 长按任意歌曲
Then 打开“歌曲选项”，显示“删除”，不存在“从最近播放移除”。

### Scenario 6：搜索只是工具
Given 用户在首页
When 点击搜索
Then 只搜索已有歌曲；没有榜单、信息流、运营推荐或社区内容。

### Scenario 7：手机音乐在 App 内选择
Given App 已获得音频读取权限
When 用户点击添加音乐
Then 不打开系统文件夹，而是在 App 内显示“选择手机音乐”面板；App 不崩溃。

## Launch / Deliverable Checklist
- Gradle Debug APK 构建成功。
- Android 35 模拟器完成全部场景验收。
- 真实截图至少覆盖：首页暂停、首页播放、音乐库。
- 人工对照用户网易云母版检查：背景深度、黑胶黑位/柔和光泽、时间字体、播放三角、进度圆点、底部导航、唱针状态。
- 自动验收检查：播放模式切换、音乐库无“最近播放/本地音乐”、播放列表长按出现“删除”。
- APK 只有在功能验收和截图验收通过后才进入交付 artifact。
- 交付包包含 APK、当前 SPEC、验收截图和 SHA256。

## Handoff Prompt For Development
请根据这份 SPEC.md 实现 Phase 1。只做 Phase 1，不做 Explicitly Out Of Scope。用户提供的网易云截图是保留区域的视觉母版；不要自由发挥。完成后按 Scenario Acceptance Tests 做 Android 35 端到端自测，并对首页暂停、首页播放和音乐库截图做母版对照。全部通过后再生成并交付 APK。
